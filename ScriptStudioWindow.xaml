<Window x:Class="OsrsMr.ScriptStudioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="osrsmr - Script Studio &amp; C# Code Editor"
        Height="700" Width="950"
        MinHeight="500" MinWidth="750"
        WindowStartupLocation="CenterScreen"
        Background="#0E1117" Foreground="#E6EDF3">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#334155"/>
                                <Setter Property="BorderBrush" Value="#00E5FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="#000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#F0F6FC"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="110"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="💻" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Bot Script Studio &amp; C# Code Editor" FontSize="16" FontWeight="Bold" Foreground="#00E5FF"/>
                        <TextBlock Text="Write, edit, and compile custom OSRS automation scripts with full API access" FontSize="11" Foreground="#8B949E"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtFilePath" Text="Scripts/Custom/MyScript.cs" Foreground="#64748B" FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Template:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#94A3B8" FontWeight="SemiBold"/>
                <ComboBox x:Name="CmbTemplates" Grid.Column="1" SelectionChanged="CmbTemplates_SelectionChanged" VerticalAlignment="Center">
                    <ComboBoxItem Content="Basic LoopScript" IsSelected="True"/>
                    <ComboBoxItem Content="Skilling &amp; Gathering"/>
                    <ComboBoxItem Content="Combat Fighter"/>
                </ComboBox>

                <Button x:Name="BtnApplyTemplate" Grid.Column="2" Content="📋 Insert Template" Click="BtnApplyTemplate_Click" Margin="8,0,0,0"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button x:Name="BtnSaveScript" Content="💾 Save Script (.cs)" Click="BtnSaveScript_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnCompileReload" Content="🔄 Reload Scripts Catalog" Background="#065F46" BorderBrush="#10B981" Foreground="#ECFDF5" Click="BtnCompileReload_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Code Editor -->
        <Border Grid.Row="2" Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="4">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="TxtCodeEditor"
                         Background="Transparent"
                         Foreground="#58A6FF"
                         CaretBrush="#00E5FF"
                         SelectionBrush="#1F6FEB"
                         BorderThickness="0"
                         FontFamily="Consolas, Cascadia Code, Courier New"
                         FontSize="13"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="NoWrap"
                         Padding="10"
                         TextChanged="TxtCodeEditor_TextChanged"/>
            </ScrollViewer>
        </Border>

        <!-- Status & Stats Bar -->
        <Grid Grid.Row="3" Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TxtLineStats" Text="Lines: 0 | Characters: 0" Foreground="#64748B" FontSize="11"/>
            <TextBlock x:Name="TxtCompileStatus" Grid.Column="1" Text="Ready" Foreground="#10B981" FontSize="11" FontWeight="SemiBold"/>
        </Grid>

        <!-- Diagnostics Console -->
        <Border Grid.Row="4" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Diagnostics &amp; Compiler Output" FontWeight="Bold" Foreground="#94A3B8" FontSize="11" Margin="0,0,0,6"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtDiagnostics" Text="[Studio] Script Studio ready. Write custom C# scripts using OsrsMr Core and Interaction APIs."
                               Foreground="#8B949E" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
